{
  "name": "mtg-ev-analyzer",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:syntax": "node --check app.js",
    "test:js": "node --test tests/scoring.test.mjs",
    "test:py": "python -m unittest discover -s tests -p 'test_*.py'",
    "test": "npm run test:syntax && npm run test:js && npm run test:py"
  }
}
