{"operation":"run","reason":"Issue #246 protected-evidence acceptance: fresh disposable branch br-blue-voice-ayx1qa7q stores the signed verification URL only in the temporary QA Worker behind a random per-run secret, retrieves it after signup completes, proves sign-in and recovery, then deletes the Worker and branch."}
