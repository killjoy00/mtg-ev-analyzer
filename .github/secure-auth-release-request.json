{"operation":"deploy-secure-auth","reason":"2026-09-19: Retry the first-party session quota hotfix. All three production functions deployed and all three Daily acceptance flows passed, but Neon briefly routed the previous draftrunapi revision during the closing marker check, so the workflow stopped before deploying the reviewed gateway. Use the same bounded settle behavior for the closing exact-revision check, then deploy the gateway and rotate the quota key."}
