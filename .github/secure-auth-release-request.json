{"operation":"deploy-secure-auth","reason":"2026-09-21: Release merged PR #244 session-rotation fix after test, e2e, and isolated backend schema gates passed. Ordinary frontend bootstrap no longer calls link-browser; deploy the worker-side no-op link guard as defense-in-depth so already-correct account/player associations reuse the valid Pack One account + CSRF session while genuine claim/merge/reassociation boundaries still rotate. Preserve #201 browser verifier exchange, #204 joined-cookie transport, and #205 CSRF Domain restoration. Rollback if account association regresses: use deploy-functions.yml to deploy the prior reviewed main revision 35b3883ddf64231e07ab39c7172dc698a86c3da3 to development, verify, then production."}
